# Резервное копирование и восстановление курсов

Эта документация описывает процесс резервного копирования и восстановления курсов в системе.

## 📦 Что сохраняется в бэкапе

Бэкап включает все данные курсов:

- **Course** - основная информация о курсах
- **CourseModule** - модули курсов
- **Lesson** - уроки в модулях
- **LessonAttachment** - вложения к урокам
- **CourseTest** - тесты
- **CourseForm** - формы
- **CourseAccessCode** - коды доступа

**НЕ сохраняется:**
- TestAttempt - попытки прохождения тестов (данные пользователей)
- FormAttempt - попытки прохождения форм (данные пользователей)
- CourseProgress - прогресс пользователей
- CourseEnrollment - подписки пользователей
- CourseReview - отзывы пользователей

## 🔄 Создание резервной копии

### На сервере (рекомендуемый способ)

```bash
cd /opt/murmur/deploy
./backup-courses.sh
```

Скрипт:
1. Копирует скрипт в контейнер backend
2. Запускает резервное копирование
3. Копирует созданный бэкап обратно на хост в `deploy/backups/`

### Вручную через контейнер

```bash
# Копируем скрипт в контейнер
docker compose -f docker-compose.prod.yml cp deploy/backup-courses.js backend:/app/backup-courses.js

# Запускаем бэкап
docker compose -f docker-compose.prod.yml exec backend node /app/backup-courses.js

# Копируем бэкап на хост
docker compose -f docker-compose.prod.yml cp backend:/app/backups/courses-backup-YYYY-MM-DDTHH-MM-SS.json deploy/backups/
```

### Локально (для разработки)

```bash
cd apps/backend
node ../../deploy/backup-courses.js
```

## 📁 Структура файла бэкапа

Бэкап сохраняется в формате JSON с следующей структурой:

```json
{
  "metadata": {
    "version": "1.0",
    "createdAt": "2024-01-15T10:30:00.000Z",
    "timestamp": 1705315800000,
    "coursesCount": 5,
    "statistics": {
      "modules": 15,
      "lessons": 45,
      "attachments": 10,
      "tests": 8,
      "forms": 12,
      "accessCodes": 3
    }
  },
  "courses": [
    {
      "id": "...",
      "slug": "...",
      "title": "...",
      "modules": [...],
      "tests": [...],
      "forms": [...],
      "accessCodes": [...]
    }
  ]
}
```

## 🔄 Восстановление курсов

### На сервере (рекомендуемый способ)

```bash
cd /opt/murmur/deploy

# Сначала проверьте, что будет восстановлено (dry-run)
./restore-courses.sh backups/courses-backup-latest.json --dry-run

# Восстановление с пропуском существующих курсов
./restore-courses.sh backups/courses-backup-latest.json --skip-existing

# Полное восстановление (перезапишет существующие курсы, если slug совпадает)
./restore-courses.sh backups/courses-backup-latest.json
```

### Вручную через контейнер

```bash
# Копируем скрипт и файл бэкапа в контейнер
docker compose -f docker-compose.prod.yml cp deploy/restore-courses.js backend:/app/restore-courses.js
docker compose -f docker-compose.prod.yml cp deploy/backups/courses-backup-latest.json backend:/app/backup-to-restore.json

# Проверка (dry-run)
docker compose -f docker-compose.prod.yml exec backend node /app/restore-courses.js /app/backup-to-restore.json --dry-run

# Восстановление
docker compose -f docker-compose.prod.yml exec backend node /app/restore-courses.js /app/backup-to-restore.json --skip-existing
```

### Локально (для разработки)

```bash
cd apps/backend
node ../../deploy/restore-courses.js ../../deploy/backups/courses-backup-latest.json --dry-run
```

## ⚙️ Опции восстановления

- `--dry-run` - только проверить, что будет восстановлено, без применения изменений
- `--skip-existing` - пропустить курсы, которые уже существуют (по slug)

## ⚠️ Важные замечания

1. **ID курсов не сохраняются** - при восстановлении создаются новые ID для всех записей
2. **Связи восстанавливаются** - все связи между модулями, уроками, тестами и формами сохраняются
3. **Slug должен быть уникальным** - если курс с таким slug уже существует, он будет пропущен (с `--skip-existing`) или вызовет ошибку
4. **Коды доступа восстанавливаются** - включая статус и информацию об активации
5. **Даты создания сохраняются** - оригинальные `createdAt` и `updatedAt` не сохраняются, используются текущие даты

## 📅 Рекомендации по расписанию

Рекомендуется создавать бэкапы:

- **Перед каждым крупным изменением** курсов
- **Ежедневно** (можно настроить через cron)
- **Перед обновлением системы** или миграциями БД

### Настройка автоматического бэкапа (cron)

Добавьте в crontab:

```bash
# Ежедневный бэкап в 3:00 ночи
0 3 * * * cd /opt/murmur/deploy && ./backup-courses.sh >> /var/log/course-backup.log 2>&1

# Хранение только последних 30 бэкапов
0 4 * * * cd /opt/murmur/deploy/backups && ls -t *.json | tail -n +31 | xargs rm -f
```

## 🔍 Проверка бэкапа

Проверить содержимое бэкапа можно так:

```bash
# Показать метаданные
cat backups/courses-backup-latest.json | jq '.metadata'

# Показать список курсов
cat backups/courses-backup-latest.json | jq '.courses[].title'

# Показать статистику
cat backups/courses-backup-latest.json | jq '.metadata.statistics'
```

## 🆘 Восстановление после потери данных

Если курсы были случайно удалены:

1. **Найдите нужный бэкап:**
   ```bash
   ls -lt deploy/backups/
   ```

2. **Проверьте содержимое бэкапа:**
   ```bash
   ./restore-courses.sh backups/courses-backup-YYYY-MM-DDTHH-MM-SS.json --dry-run
   ```

3. **Восстановите курсы:**
   ```bash
   ./restore-courses.sh backups/courses-backup-YYYY-MM-DDTHH-MM-SS.json --skip-existing
   ```

4. **Проверьте результат:**
   - Откройте админ-панель
   - Убедитесь, что все курсы восстановлены
   - Проверьте структуру модулей и уроков

## 📝 Примечания

- Бэкапы сохраняются в `deploy/backups/`
- Создается симлинк `courses-backup-latest.json` на последний бэкап
- Размер бэкапа зависит от количества курсов и их содержимого
- Рекомендуется хранить бэкапы в отдельном месте (например, S3, другой сервер)

