# Быстрый старт: Резервное копирование курсов

## 📦 Создание бэкапа

```bash
cd /opt/murmur/deploy
./backup-courses.sh
```

Бэкап будет сохранен в `deploy/backups/courses-backup-YYYY-MM-DDTHH-MM-SS.json`

## 🔄 Восстановление

```bash
cd /opt/murmur/deploy

# 1. Проверка (что будет восстановлено)
./restore-courses.sh backups/courses-backup-latest.json --dry-run

# 2. Восстановление (пропустить существующие)
./restore-courses.sh backups/courses-backup-latest.json --skip-existing
```

## 📋 Список бэкапов

```bash
ls -lt deploy/backups/*.json
```

## ⚠️ Важно

- Бэкапы сохраняют **только структуру курсов** (модули, уроки, тесты, формы)
- **НЕ сохраняются** данные пользователей (прогресс, попытки, подписки)
- При восстановлении создаются **новые ID** для всех записей
- Курсы с одинаковым `slug` будут пропущены (с `--skip-existing`)

## 📚 Подробная документация

См. `COURSE_BACKUP.md` для полной документации.

